import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
  },
  noRecipesText: {
    textAlign: "center",
    fontSize: 15,
    marginTop: 250,
  },
});
